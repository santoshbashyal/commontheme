export interface Isettings {
  message: string;
  settings: Settings;
}

export interface Settings {
  site_title: string;
  logo: string;
  fabcon: string;
  social_facebook: string;
  social_youtube: string;
}
